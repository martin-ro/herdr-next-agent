id = "martinro.next-agent"
name = "Next Agent"
version = "0.1.0"
min_herdr_version = "0.7.0"
platforms = ["linux"]

[[actions]]
id = "jump"
title = "Jump to next agent needing attention"
contexts = ["global", "workspace"]
command = ["python3", "next_agent.py"]
